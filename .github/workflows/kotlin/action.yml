name: Build Kotlin Package
runs:
  using: composite
  steps:
    - name: Run setup
      working-directory: ./wrappers/kotlin
      shell: bash
      run: sh setup.sh

    - name: Build
      working-directory: ./wrappers/kotlin
      shell: bash
      run: make

    # TODO test and package into an artifact

